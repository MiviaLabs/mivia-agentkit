version: 1
name: research-loop
bound: iterations
max_iterations: 3
steps:
  - id: research
    producer: {{ .Routing.DefaultProducer }}
    artifact: research.md
  - id: review
    reviewers:
{{- range .Routing.DefaultReviewers }}
      - {{ . }}
{{- end }}
    artifact: research.md
    consensus:
      mode: majority
      tie_breaker: strict
      min_reviewers: {{ .Routing.MinReviewers }}
    on_fail: iterate
exit_when: review-pass
on_exhausted: warn
